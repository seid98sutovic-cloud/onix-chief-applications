# Server
PORT=3000
BASE_URL=http://localhost:3000

# Admin panel login
ADMIN_USERNAME=admin
ADMIN_PASSWORD=OnixChief2026!

# JWT secret — generate: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=replace-with-long-random-secret

# Gmail SMTP (App Password, NOT regular password)
# Create at: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=seid98sutovic@gmail.com
SMTP_PASS=your-16-char-app-password
NOTIFY_EMAIL=seid98sutovic@gmail.com
EMAIL_FROM="ONIX Roleplay <seid98sutovic@gmail.com>"
